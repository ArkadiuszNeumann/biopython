name: pullrequest

on:
  pull_request:
    branches:
      - master

jobs:
  update-verion:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: update version
        if:  github.event.pull_request.merged == true
        run: |
        
